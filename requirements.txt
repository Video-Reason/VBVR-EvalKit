# Core dependencies
numpy>=1.24.0
Pillow>=10.0.0
opencv-python>=4.8.0
tqdm>=4.66.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# API client dependencies
requests>=2.31.0
httpx>=0.25.0
aiohttp>=3.9.0
tenacity>=8.2.0  # for retry logic
runwayml>=3.17.0  # for Runway ML I2V generation

# Google Cloud dependencies for Veo
google-cloud-aiplatform>=1.38.0
google-cloud-storage>=2.10.0  # For GCS video storage
google-auth>=2.23.0
google-auth-httplib2>=0.1.1

# Video processing
imageio>=2.33.0
imageio-ffmpeg>=0.4.9
moviepy>=1.0.3
cairosvg>=2.7.0

# Evaluation metrics
scikit-image>=0.22.0
scipy>=1.11.0
torch>=2.0.0  # for perceptual metrics
torchvision>=0.15.0

# Data handling (minimal - only for evaluation result processing)
pandas>=2.1.0  # Used for evaluation result processing
# matplotlib and seaborn removed - analysis moved to external tools

# Evaluation interfaces  
gradio>=4.0.0  # Used for human evaluation interface

# Note: Task generation dependencies removed - VMEvalKit now focuses on inference/evaluation
# Maze generation dependencies moved to external data generation repositories

# Logging and monitoring
loguru>=0.7.2
rich>=13.7.0  # for beautiful terminal output

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Type checking
mypy>=1.7.0
types-requests>=2.31.0

# Documentation dependencies removed

# AWS
boto3>=1.34.0

# HuggingFace (optional - for dataset sharing)
huggingface_hub>=0.24.0
datasets>=2.18.0  # For structured dataset upload
pyarrow>=14.0.2  # Required by datasets

# Web dependencies removed - no web dashboard in focused version

# Core inference and evaluation dependencies only
transformers>=4.55.2  # Used by some model implementations
